<?xml version="1.0" encoding="UTF-8"?><rss version="2.0" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Hyper Threading - Blog</title><description>超线程的个人博客网站</description><link>https://hyperthreading.cn/</link><language>zh_CN</language><item><title>FNS 部署指南与踩坑笔记</title><link>https://hyperthreading.cn/posts/fns_%E9%83%A8%E7%BD%B2%E6%8C%87%E5%8D%97_%E8%B8%A9%E5%9D%91%E7%AC%94%E8%AE%B0/</link><guid isPermaLink="true">https://hyperthreading.cn/posts/fns_%E9%83%A8%E7%BD%B2%E6%8C%87%E5%8D%97_%E8%B8%A9%E5%9D%91%E7%AC%94%E8%AE%B0/</guid><description>记录 Fast Note Sync Service 在 Ubuntu 服务器上的部署流程、Nginx Proxy Manager 反代配置，以及 Cloudflare、Obsidian 插件接入中的常见踩坑与解决方案。</description><pubDate>Tue, 28 Apr 2026 00:00:00 GMT</pubDate><content:encoded>&lt;h1&gt;Fast Note Sync Service (FNS) 部署指南 &amp;amp; 踩坑笔记&lt;/h1&gt;
&lt;blockquote&gt;
&lt;p&gt;基于 &lt;a href=&quot;https://github.com/haierkeys/fast-note-sync-service&quot;&gt;haierkeys/fast-note-sync-service&lt;/a&gt; 官方仓库 + 真实部署踩坑记录整理&lt;br /&gt;
环境：百度云 VPS / Ubuntu / Nginx Proxy Manager (Docker) / Cloudflare CDN&lt;/p&gt;
&lt;/blockquote&gt;
&lt;hr /&gt;
&lt;h2&gt;一、快速安装&lt;/h2&gt;
&lt;h3&gt;方式 1：一键脚本（推荐）&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;# 海外服务器
bash &amp;lt;(curl -fsSL https://raw.githubusercontent.com/haierkeys/fast-note-sync-service/master/scripts/quest_install.sh)

# 国内服务器（腾讯云 CNB 镜像，速度快）
bash &amp;lt;(curl -fsSL https://cnb.cool/haierkeys/fast-note-sync-service/-/git/raw/master/scripts/quest_install.sh) --cnb
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;脚本会自动完成：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;下载对应系统的二进制文件到 &lt;code&gt;/opt/fast-note&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;创建全局快捷命令 &lt;code&gt;fns&lt;/code&gt;（位于 &lt;code&gt;/usr/local/bin/fns&lt;/code&gt;）&lt;/li&gt;
&lt;li&gt;注册 Systemd 服务并设置开机自启&lt;/li&gt;
&lt;li&gt;进入交互式菜单，支持安装/升级/启停/切换镜像源&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;常用管理命令：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;fns status    # 查看服务状态
fns start     # 启动服务
fns stop      # 停止服务
fns update    # 升级版本
fns menu      # 进入交互菜单
&lt;/code&gt;&lt;/pre&gt;
&lt;hr /&gt;
&lt;h2&gt;二、首次启动必做检查&lt;/h2&gt;
&lt;h3&gt;1. 放行本地防火墙（ufw）&lt;/h3&gt;
&lt;p&gt;安装脚本&lt;strong&gt;不会自动放行 ufw&lt;/strong&gt;，这是最常见的&quot;服务启动但端口不通&quot;原因：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;ufw allow 9000/tcp
ufw reload
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;2. 放行云服务器安全组&lt;/h3&gt;
&lt;p&gt;登录云厂商控制台（阿里云/腾讯云/百度云/AWS），在&lt;strong&gt;安全组入方向&lt;/strong&gt;添加：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;协议：TCP&lt;/li&gt;
&lt;li&gt;端口：&lt;code&gt;9000&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;源地址：&lt;code&gt;0.0.0.0/0&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;3. 修改默认 Token 密钥（安全警告）&lt;/h3&gt;
&lt;p&gt;启动日志会提示：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;WARN  Using default secret key - please change security.auth-token-key
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;生成安全密钥并修改配置：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;openssl rand -base64 32
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;编辑 &lt;code&gt;/opt/fast-note/config/config.yaml&lt;/code&gt;：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;security:
    auth-token-key: &quot;你生成的密钥&quot;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;重启服务生效&lt;/strong&gt;（见下方&quot;服务重启步骤&quot;）。&lt;/p&gt;
&lt;hr /&gt;
&lt;h2&gt;三、服务重启步骤&lt;/h2&gt;
&lt;p&gt;⚠️ &lt;code&gt;systemctl restart fast-note&lt;/code&gt; &lt;strong&gt;不适用于此安装方式&lt;/strong&gt;。&lt;/p&gt;
&lt;p&gt;正确步骤：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;fns        # 唤起交互面板
# 然后输入 3 停止服务
# 再输入 2 启动服务
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;或直接使用：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;fns stop   # 停止
fns start  # 启动
&lt;/code&gt;&lt;/pre&gt;
&lt;hr /&gt;
&lt;h2&gt;四、配置域名 + HTTPS（Nginx Proxy Manager）&lt;/h2&gt;
&lt;h3&gt;前置条件&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;已安装 &lt;a href=&quot;https://nginxproxymanager.com/&quot;&gt;Nginx Proxy Manager&lt;/a&gt;（Docker 部署）&lt;/li&gt;
&lt;li&gt;域名 DNS 已解析到服务器公网 IP&lt;/li&gt;
&lt;li&gt;如使用 Cloudflare CDN，代理状态（橙色云）可开可关&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;NPM 配置步骤&lt;/h3&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;登录 NPM 后台&lt;/strong&gt;（默认 &lt;code&gt;http://服务器IP:81&lt;/code&gt;）&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Proxy Hosts → Add Proxy Host&lt;/strong&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;字段&lt;/th&gt;
&lt;th&gt;值&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;Domain Names&lt;/td&gt;
&lt;td&gt;&lt;code&gt;notesync.yourdomain.com&lt;/code&gt;&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Scheme&lt;/td&gt;
&lt;td&gt;&lt;code&gt;http&lt;/code&gt;&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Forward Hostname / IP&lt;/td&gt;
&lt;td&gt;&lt;code&gt;172.17.0.1&lt;/code&gt;（Docker 网关地址，NPM 容器访问宿主机）&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Forward Port&lt;/td&gt;
&lt;td&gt;&lt;code&gt;9000&lt;/code&gt;&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Websockets Support&lt;/td&gt;
&lt;td&gt;&lt;strong&gt;✅ 必须勾选&lt;/strong&gt;（同步接口 &lt;code&gt;/api/user/sync&lt;/code&gt; 使用 WS）&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Block Common Exploits&lt;/td&gt;
&lt;td&gt;建议勾选&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;SSL 选项卡&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;SSL Certificate: &lt;code&gt;Request a new SSL Certificate&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;Force SSL: &lt;strong&gt;按需勾选&lt;/strong&gt;（见下方踩坑 #4）&lt;/li&gt;
&lt;li&gt;HTTP/2 Support: 勾选&lt;/li&gt;
&lt;li&gt;如 Cloudflare 代理开启，建议用 &lt;strong&gt;DNS Challenge&lt;/strong&gt; 申请证书，Provider 选 Cloudflare&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;Advanced 自定义配置&lt;/strong&gt;（必须加，来自官方示例）&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;进入 Advanced → Custom Nginx Configuration，粘贴：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;client_max_body_size 50m;
proxy_buffering off;
proxy_max_temp_file_size 0;
proxy_read_timeout 86400;
proxy_send_timeout 86400;
&lt;/code&gt;&lt;/pre&gt;
&lt;blockquote&gt;
&lt;p&gt;说明：&lt;code&gt;client_max_body_size&lt;/code&gt; 控制附件上传大小；&lt;code&gt;proxy_buffering off&lt;/code&gt; 保证 WebSocket/大文件实时传输不缓冲。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;保存并测试&lt;/strong&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;浏览器访问 &lt;code&gt;https://notesync.yourdomain.com/webgui&lt;/code&gt;&lt;/p&gt;
&lt;hr /&gt;
&lt;h2&gt;五、配置 ext-api-url（关键）&lt;/h2&gt;
&lt;p&gt;编辑 &lt;code&gt;/opt/fast-note/config/config.yaml&lt;/code&gt;：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;server:
    # 如果你只用域名访问，填域名
    ext-api-url: &quot;https://notesync.yourdomain.com&quot;
    share-base-url: &quot;https://notesync.yourdomain.com&quot;

    # 如果你需要 IP 和域名同时可用，留空让前端自适应
    # ext-api-url: &quot;&quot;
    # share-base-url: &quot;&quot;
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;按&quot;服务重启步骤&quot;重启生效。&lt;/p&gt;
&lt;hr /&gt;
&lt;h2&gt;六、Obsidian 插件使用方法&lt;/h2&gt;
&lt;h3&gt;1. 安装插件&lt;/h3&gt;
&lt;p&gt;在 Obsidian 中：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;方式 A&lt;/strong&gt;：通过 Obsidian 社区插件市场搜索 &lt;code&gt;Fast Note Sync&lt;/code&gt; 直接安装&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;方式 B&lt;/strong&gt;：手动下载 &lt;a href=&quot;https://github.com/haierkeys/obsidian-fast-note-sync&quot;&gt;obsidian-fast-note-sync&lt;/a&gt; 最新 Release，解压到 &lt;code&gt;.obsidian/plugins/obsidian-fast-note-sync/&lt;/code&gt; 目录&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;安装后在 Obsidian 设置 → 第三方插件中启用 &lt;code&gt;Fast Note Sync&lt;/code&gt;。&lt;/p&gt;
&lt;h3&gt;2. 获取服务器 API 配置&lt;/h3&gt;
&lt;ol&gt;
&lt;li&gt;浏览器打开 FNS WebGUI：&lt;code&gt;https://notesync.yourdomain.com/webgui&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;首次使用需&lt;strong&gt;注册账号&lt;/strong&gt;（管理员账号，默认 UID 为 1）&lt;/li&gt;
&lt;li&gt;登录后，点击页面上的 &lt;strong&gt;&quot;复制 API 配置&quot;&lt;/strong&gt;（或类似按钮）&lt;/li&gt;
&lt;li&gt;配置内容会自动复制到剪贴板，格式类似：&lt;/li&gt;
&lt;/ol&gt;
&lt;pre&gt;&lt;code&gt;{
  &quot;serverUrl&quot;: &quot;https://notesync.yourdomain.com&quot;,
  &quot;token&quot;: &quot;Bearer xxxxxxxx&quot;,
  &quot;vault&quot;: &quot;defaultVault&quot;
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;3. 粘贴到 Obsidian 插件设置&lt;/h3&gt;
&lt;ol&gt;
&lt;li&gt;打开 Obsidian → 设置 → 第三方插件 → Fast Note Sync&lt;/li&gt;
&lt;li&gt;将复制的 API 配置&lt;strong&gt;完整粘贴&lt;/strong&gt;到插件的配置输入框中&lt;/li&gt;
&lt;li&gt;插件会自动解析 &lt;code&gt;serverUrl&lt;/code&gt;、&lt;code&gt;token&lt;/code&gt;、&lt;code&gt;vault&lt;/code&gt; 等字段&lt;/li&gt;
&lt;li&gt;保存设置&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;4. 首次同步&lt;/h3&gt;
&lt;ol&gt;
&lt;li&gt;在 Obsidian 中打开命令面板（Ctrl/Cmd + P）&lt;/li&gt;
&lt;li&gt;搜索 &lt;code&gt;Fast Note Sync: 同步全部&lt;/code&gt; 或点击左侧栏的同步图标&lt;/li&gt;
&lt;li&gt;插件会自动创建 Vault（如 &lt;code&gt;defaultVault&lt;/code&gt;）并开始双向同步&lt;/li&gt;
&lt;li&gt;后续编辑会自动实时同步到服务器和其他设备&lt;/li&gt;
&lt;/ol&gt;
&lt;h3&gt;5. 多设备同步&lt;/h3&gt;
&lt;p&gt;在其他设备的 Obsidian 中：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;同样安装并启用插件&lt;/li&gt;
&lt;li&gt;粘贴&lt;strong&gt;同一套 API 配置&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;首次同步会把服务器上的笔记拉取到本地&lt;/li&gt;
&lt;li&gt;之后任意设备的编辑都会实时同步到所有在线设备&lt;/li&gt;
&lt;/ol&gt;
&lt;hr /&gt;
&lt;h2&gt;七、踩坑笔记（Troubleshooting）&lt;/h2&gt;
&lt;h3&gt;坑 1：服务启动但 9000 端口无法访问&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;现象&lt;/strong&gt;：&lt;code&gt;fns status&lt;/code&gt; 显示运行中，但浏览器/ curl 访问超时。&lt;br /&gt;
&lt;strong&gt;原因&lt;/strong&gt;：&lt;code&gt;ufw&lt;/code&gt; 或云安全组未放行 9000。&lt;br /&gt;
&lt;strong&gt;解决&lt;/strong&gt;：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;ufw allow 9000/tcp
# 同时检查云控制台安全组
&lt;/code&gt;&lt;/pre&gt;
&lt;hr /&gt;
&lt;h3&gt;坑 2：GitHub 支持文档下载超时&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;现象&lt;/strong&gt;：日志反复出现：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;WARN  Failed to fetch support records ... context deadline exceeded
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;原因&lt;/strong&gt;：服务器无法访问 &lt;code&gt;raw.githubusercontent.com&lt;/code&gt;（国内常见）。&lt;br /&gt;
&lt;strong&gt;影响&lt;/strong&gt;：仅影响 WebGUI 的帮助文档加载，&lt;strong&gt;不影响核心同步功能&lt;/strong&gt;。&lt;br /&gt;
&lt;strong&gt;解决&lt;/strong&gt;（可选）：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;给服务器配置代理访问 GitHub&lt;/li&gt;
&lt;li&gt;或忽略该警告&lt;/li&gt;
&lt;/ul&gt;
&lt;hr /&gt;
&lt;h3&gt;坑 3：修改 ext-api-url 后，IP 访问出现 &quot;Failed to fetch&quot;&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;现象&lt;/strong&gt;：把 &lt;code&gt;ext-api-url&lt;/code&gt; 改成域名后，用 &lt;code&gt;http://IP:9000/webgui&lt;/code&gt; 打开报错。&lt;br /&gt;
&lt;strong&gt;原因&lt;/strong&gt;：前端页面会强制向 &lt;code&gt;ext-api-url&lt;/code&gt; 指定的域名发起 API 请求，跨域或协议不匹配导致失败。&lt;br /&gt;
&lt;strong&gt;解决&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;方案 A（推荐生产环境）&lt;/strong&gt;：统一使用域名访问，不再直接用 IP&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;方案 B（IP/域名共存）&lt;/strong&gt;：把 &lt;code&gt;ext-api-url&lt;/code&gt; 和 &lt;code&gt;share-base-url&lt;/code&gt; 留空 &lt;code&gt;&quot;&quot;&lt;/code&gt;，让前端自适应当前浏览器地址&lt;/li&gt;
&lt;/ul&gt;
&lt;hr /&gt;
&lt;h3&gt;坑 4：Cloudflare + NPM 导致 ERR_TOO_MANY_REDIRECTS&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;现象&lt;/strong&gt;：浏览器提示&quot;重定向次数过多&quot;。&lt;br /&gt;
&lt;strong&gt;原因&lt;/strong&gt;：Cloudflare SSL/TLS 加密模式为 &lt;strong&gt;Flexible&lt;/strong&gt; 时，回源使用 HTTP；而 NPM 开启了 &lt;strong&gt;Force SSL&lt;/strong&gt;，检测到 HTTP 就 301 跳 HTTPS，形成死循环。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;解决&lt;/strong&gt;（二选一）：&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;方案&lt;/th&gt;
&lt;th&gt;操作&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;&lt;strong&gt;推荐&lt;/strong&gt;&lt;/td&gt;
&lt;td&gt;Cloudflare 后台 → SSL/TLS → Overview → 加密模式改为 &lt;strong&gt;Full (strict)&lt;/strong&gt;&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;备选&lt;/td&gt;
&lt;td&gt;NPM 中关闭 &lt;strong&gt;Force SSL&lt;/strong&gt;，让 Cloudflare 自行处理 HTTPS 跳转&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;hr /&gt;
&lt;h3&gt;坑 5：NPM 中 Forward IP 填 127.0.0.1 不通&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;现象&lt;/strong&gt;：NPM 返回 502 Bad Gateway。&lt;br /&gt;
&lt;strong&gt;原因&lt;/strong&gt;：NPM 是 Docker 容器，&lt;code&gt;127.0.0.1&lt;/code&gt; 指向的是容器自身，不是宿主机。&lt;br /&gt;
&lt;strong&gt;解决&lt;/strong&gt;：填写 Docker 网关地址 &lt;code&gt;172.17.0.1&lt;/code&gt;，或宿主机的内网 IP（如 &lt;code&gt;192.168.x.x&lt;/code&gt;）。&lt;/p&gt;
&lt;hr /&gt;
&lt;h3&gt;坑 6：SSL 证书申请失败（Cloudflare 代理开启时）&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;现象&lt;/strong&gt;：NPM 申请 Let&apos;s Encrypt 证书报错或超时。&lt;br /&gt;
&lt;strong&gt;原因&lt;/strong&gt;：Cloudflare 橙色云（代理开启）会拦截 HTTP-01 验证请求。&lt;br /&gt;
&lt;strong&gt;解决&lt;/strong&gt;：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;方案 A&lt;/strong&gt;：申请证书时临时关闭 Cloudflare 代理（灰色云），申请完再打开&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;方案 B（推荐）&lt;/strong&gt;：NPM 中使用 &lt;strong&gt;DNS Challenge&lt;/strong&gt;，Provider 选 Cloudflare，填入 API Token（需 Zone:Read + DNS:Edit 权限）&lt;/li&gt;
&lt;/ul&gt;
&lt;hr /&gt;
&lt;h3&gt;坑 7：大文件/附件同步失败或超时&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;现象&lt;/strong&gt;：上传大图片/附件时中断。&lt;br /&gt;
&lt;strong&gt;原因&lt;/strong&gt;：NPM 默认 &lt;code&gt;client_max_body_size&lt;/code&gt; 太小，或 &lt;code&gt;proxy_buffering&lt;/code&gt; 开启导致内存缓冲。&lt;br /&gt;
&lt;strong&gt;解决&lt;/strong&gt;：在 NPM Advanced 中加上官方推荐的配置：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;client_max_body_size 50m;
proxy_buffering off;
proxy_max_temp_file_size 0;
&lt;/code&gt;&lt;/pre&gt;
&lt;hr /&gt;
&lt;h3&gt;坑 8：Obsidian 插件连接不上&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;检查清单&lt;/strong&gt;：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;服务器 9000 端口是否放行（ufw + 安全组）&lt;/li&gt;
&lt;li&gt;如使用域名，确认 &lt;code&gt;ext-api-url&lt;/code&gt; 配置正确&lt;/li&gt;
&lt;li&gt;如使用 HTTPS，确认证书有效（不是自签）&lt;/li&gt;
&lt;li&gt;如使用 Cloudflare，确认加密模式为 Full (strict)&lt;/li&gt;
&lt;li&gt;在 WebGUI 中点击&quot;复制 API 配置&quot;，完整粘贴到 Obsidian 插件设置中&lt;/li&gt;
&lt;li&gt;确认插件中的 &lt;code&gt;serverUrl&lt;/code&gt; 与浏览器访问的地址一致（不要混用 IP 和域名）&lt;/li&gt;
&lt;/ol&gt;
&lt;hr /&gt;
&lt;h2&gt;八、官方参考链接&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;FNS 仓库主页：https://github.com/haierkeys/fast-note-sync-service&lt;/li&gt;
&lt;li&gt;国内镜像：https://cnb.cool/haierkeys/fast-note-sync-service&lt;/li&gt;
&lt;li&gt;官方 Nginx 配置示例：https://github.com/haierkeys/fast-note-sync-service/blob/master/scripts/https-nginx-example.conf&lt;/li&gt;
&lt;li&gt;完整配置示例：https://github.com/haierkeys/fast-note-sync-service/blob/master/config/config.yaml&lt;/li&gt;
&lt;li&gt;Obsidian 插件仓库：https://github.com/haierkeys/obsidian-fast-note-sync&lt;/li&gt;
&lt;/ul&gt;
</content:encoded></item><item><title>卷积神经网络（CNN）</title><link>https://hyperthreading.cn/posts/%E5%8D%B7%E7%A7%AF%E7%A5%9E%E7%BB%8F%E7%BD%91%E7%BB%9Ccnn/</link><guid isPermaLink="true">https://hyperthreading.cn/posts/%E5%8D%B7%E7%A7%AF%E7%A5%9E%E7%BB%8F%E7%BD%91%E7%BB%9Ccnn/</guid><pubDate>Mon, 01 Dec 2025 00:00:00 GMT</pubDate><content:encoded>&lt;h2&gt;🌟 卷积神经网络（CNN）终极详解&lt;/h2&gt;
&lt;h2&gt;一、CNN 的生物学灵感与哲学思想&lt;/h2&gt;
&lt;h3&gt;1.1 生物视觉皮层启发&lt;/h3&gt;
&lt;p&gt;CNN 的设计灵感来源于 1960 年代神经科学家 Hubel 和 Wiesel 对猫视觉皮层的研究：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;简单细胞（Simple Cells）：只对特定位置、方向的边缘/条状刺激有反应 → 对应 CNN 的“卷积核”&lt;/li&gt;
&lt;li&gt;复杂细胞（Complex Cells）：对某方向边缘有反应，但位置不敏感 → 对应“池化层”的平移不变性&lt;/li&gt;
&lt;li&gt;层级结构：低级特征（边缘）→ 中级特征（形状）→ 高级特征（物体）→ 对应 CNN 的“层叠结构”&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;🧬 CNN 是对生物视觉系统的“工程化模拟”，不是完全复制，而是提取其核心思想：局部感受、层次抽象、权值共享 。&lt;/p&gt;
&lt;h3&gt;1.2 哲学思想：让机器“学会看”&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;传统方法：人工设计特征（如SIFT、HOG）→ 费时费力，泛化差&lt;/li&gt;
&lt;li&gt;CNN 方法：端到端学习 ，从原始像素自动学习最优特征表示&lt;/li&gt;
&lt;li&gt;核心哲学：Representation Learning（表示学习）&lt;/li&gt;
&lt;/ul&gt;
&lt;hr /&gt;
&lt;h2&gt;二、卷积操作的数学本质（超详细）&lt;/h2&gt;
&lt;h3&gt;2.1 离散卷积公式（2D）&lt;/h3&gt;
&lt;p&gt;对输入图像 $$ I \in \mathbb{R}^{H \times W} $$ 和卷积核 $$K \in \mathbb{R}^{k \times k} $$ ，输出特征图 $$ O $$ 在位置 $$ (i,j) $$ 的值为：&lt;/p&gt;
&lt;p&gt;$$O(i,j) = (I * K)(i,j) = \sum_{m=0}^{k-1} \sum_{n=0}^{k-1} I(i+m, j+n) \cdot K(m,n)$$&lt;/p&gt;
&lt;p&gt;注意：实际深度学习框架中多为“互相关（cross-correlation）”，即不翻转核，但习惯仍称“卷积”。&lt;/p&gt;
&lt;h3&gt;2.2 多通道输入/输出&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;输入通道数：$$C_{in} $$（如RGB=3）&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;输出通道数：$$ C_{out}$$（即卷积核个数）&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;每个卷积核尺寸：$$ k \times k \times C_{in} $$&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;总参数量：$$C_{out} \times (k \times k \times C_{in} + 1)$$（+1 是偏置项）&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;✅ 举例：&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;输入：224×224×3&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;卷积层：64个 3×3 卷积核&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;参数量 = $$ 64 \times (3 \times 3 \times 3 + 1) = 64 \times 28 = 1,792 $$&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;对比全连接：$$224 \times 224 \times 3 \times 1000 = 150M$$→ CNN 参数效率极高！&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;2.3 输出尺寸计算公式&lt;/h3&gt;
&lt;p&gt;给定：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;输入尺寸：$$ W_{in} \times H_{in} $$&lt;/li&gt;
&lt;li&gt;卷积核尺寸：$$ K $$&lt;/li&gt;
&lt;li&gt;步长：$$ S $$&lt;/li&gt;
&lt;li&gt;填充：$$ P $$&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;输出尺寸：&lt;/p&gt;
&lt;p&gt;$$W_{out} = \left\lfloor \frac{W_{in} + 2P - K}{S} \right\rfloor + 1$$&lt;/p&gt;
&lt;p&gt;📌 常用设置：K=3, S=1, P=1 → 尺寸不变；K=3, S=2, P=1 → 尺寸减半&lt;/p&gt;
&lt;hr /&gt;
&lt;h2&gt;三、CNN 各层组件深度剖析&lt;/h2&gt;
&lt;h3&gt;3.1 卷积层（Conv Layer）——不止是“滑动窗口”&lt;/h3&gt;
&lt;h4&gt;类型扩展&lt;/h4&gt;
&lt;ul&gt;
&lt;li&gt;标准卷积（Standard Conv）：上述基本形式&lt;/li&gt;
&lt;li&gt;空洞卷积（Dilated Conv）：在核元素间插入“空洞”，扩大感受野而不增加参数。用于语义分割（如DeepLab）&lt;/li&gt;
&lt;li&gt;转置卷积（Transposed Conv）：又称“反卷积”，用于上采样（如图像生成、分割）&lt;/li&gt;
&lt;li&gt;深度可分离卷积（Depthwise Separable Conv）：
&lt;ul&gt;
&lt;li&gt;Step1: Depthwise Conv —— 每个输入通道单独卷积&lt;/li&gt;
&lt;li&gt;Step2: Pointwise Conv —— 1×1卷积融合通道&lt;/li&gt;
&lt;li&gt;参数量大幅减少 → 用于MobileNet等轻量模型&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;h4&gt;感受野（Receptive Field）&lt;/h4&gt;
&lt;ul&gt;
&lt;li&gt;定义：输出特征图上某一点，对应输入图像的区域大小&lt;/li&gt;
&lt;li&gt;随着网络加深，感受野增大 → 高层神经元“看到”更大范围&lt;/li&gt;
&lt;li&gt;计算公式（简化）：若每层 stride=1，则第L层感受野 ≈ $$ 1 + (K-1) \times L $$&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;🎯 目标检测中，大感受野对检测大物体至关重要。&lt;/p&gt;
&lt;hr /&gt;
&lt;h3&gt;3.2 激活函数 —— 为什么ReLU是王者？&lt;/h3&gt;
&lt;h4&gt;常见激活函数对比&lt;/h4&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;函数&lt;/th&gt;
&lt;th&gt;公式&lt;/th&gt;
&lt;th&gt;优点&lt;/th&gt;
&lt;th&gt;缺点&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;Sigmoid&lt;/td&gt;
&lt;td&gt;$$ \frac{1}{1+e^{-x}} $$&lt;/td&gt;
&lt;td&gt;输出0~1，可解释概率&lt;/td&gt;
&lt;td&gt;梯度消失、非零中心&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Tanh&lt;/td&gt;
&lt;td&gt;$$ \frac{e^x - e^{-x}}{e^x + e^{-x}} $$&lt;/td&gt;
&lt;td&gt;零中心&lt;/td&gt;
&lt;td&gt;梯度消失&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;ReLU&lt;/td&gt;
&lt;td&gt;$$ \max(0,x) $$&lt;/td&gt;
&lt;td&gt;计算快、缓解梯度消失&lt;/td&gt;
&lt;td&gt;“死神经元”问题&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Leaky ReLU&lt;/td&gt;
&lt;td&gt;$$ \max(0.01x, x) $$&lt;/td&gt;
&lt;td&gt;解决死神经元&lt;/td&gt;
&lt;td&gt;需调参&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;ELU&lt;/td&gt;
&lt;td&gt;$$ x&amp;gt;0: x; x≤0: \alpha(e^x-1) $$&lt;/td&gt;
&lt;td&gt;负值有梯度，均值≈0&lt;/td&gt;
&lt;td&gt;计算稍慢&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;✅ 实践建议：默认用ReLU，如遇神经元“死亡”，换Leaky ReLU或ELU。&lt;/p&gt;
&lt;hr /&gt;
&lt;h3&gt;3.3 池化层 —— 为什么Max Pooling最常用？&lt;/h3&gt;
&lt;h4&gt;Max Pooling vs Average Pooling&lt;/h4&gt;
&lt;ul&gt;
&lt;li&gt;Max Pooling：保留最显著特征，增强纹理/边缘响应 → 更适合分类&lt;/li&gt;
&lt;li&gt;Average Pooling：平滑特征，保留背景信息 → 有时用于最后全局池化&lt;/li&gt;
&lt;/ul&gt;
&lt;h4&gt;全局平均池化（Global Average Pooling, GAP）&lt;/h4&gt;
&lt;ul&gt;
&lt;li&gt;对每个特征图求平均，得到一个值 → 替代全连接层&lt;/li&gt;
&lt;li&gt;优点：无参数、防过拟合、可解释性强（CAM可视化）&lt;/li&gt;
&lt;li&gt;应用：GoogLeNet、ResNet等现代架构&lt;/li&gt;
&lt;/ul&gt;
&lt;hr /&gt;
&lt;h3&gt;3.4 批归一化（Batch Normalization, BN）——训练加速神器&lt;/h3&gt;
&lt;h4&gt;为什么需要 BN？&lt;/h4&gt;
&lt;p&gt;深度网络中，层间输入分布会不断变化（Internal Covariate Shift）→ 训练不稳定、需小学习率。&lt;/p&gt;
&lt;h4&gt;BN公式（训练时）&lt;/h4&gt;
&lt;p&gt;对一个batch的某通道数据 $$x$$：&lt;/p&gt;
&lt;p&gt;$$\hat{x} = \frac{x - \mu_B}{\sqrt{\sigma_B^2 + \epsilon}}, \quad y = \gamma \hat{x} + \beta$$&lt;/p&gt;
&lt;p&gt;其中 $$ \mu_B, \sigma_B^2 $$ 是batch均值和方差，$$\gamma, \beta$$ 是可学习参数。&lt;/p&gt;
&lt;h4&gt;作用&lt;/h4&gt;
&lt;ul&gt;
&lt;li&gt;加速收敛（可用大学习率）&lt;/li&gt;
&lt;li&gt;一定程度替代Dropout（正则化效果）&lt;/li&gt;
&lt;li&gt;提高模型鲁棒性&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;📌 实践：通常在卷积后、激活前 插入BN层（Conv → BN → ReLU）&lt;/p&gt;
&lt;hr /&gt;
&lt;h3&gt;3.5 全连接层与Softmax —— 最后的决策者&lt;/h3&gt;
&lt;h4&gt;Softmax函数&lt;/h4&gt;
&lt;p&gt;将网络输出 $$ z = [z_1, z_2, ..., z_K] $$ 转为概率分布：&lt;/p&gt;
&lt;p&gt;$$p_i = \frac{e^{z_i}}{\sum_{j=1}^K e^{z_j}}$$&lt;/p&gt;
&lt;h4&gt;损失函数：交叉熵（Cross-Entropy）&lt;/h4&gt;
&lt;p&gt;$$\mathcal{L} = -\sum_{i=1}^K y_i \log(p_i)$$&lt;/p&gt;
&lt;p&gt;其中 $$ y_i $$ 是真实标签的one-hot编码。&lt;/p&gt;
&lt;p&gt;✅ 交叉熵 + Softmax 是分类任务的黄金搭档。&lt;/p&gt;
&lt;hr /&gt;
&lt;h2&gt;四、CNN 训练全流程详解&lt;/h2&gt;
&lt;h3&gt;4.1 数据预处理&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;归一化：像素值 /255.0 → [0,1] 或 标准化 $$ (x - mean)/std $$&lt;/li&gt;
&lt;li&gt;数据增强（Data Augmentation）：
&lt;ul&gt;
&lt;li&gt;随机裁剪、旋转、翻转、色彩抖动、CutMix、MixUp&lt;/li&gt;
&lt;li&gt;本质：增加数据多样性，提升泛化，防过拟合&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;4.2 优化器选择&lt;/h3&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;优化器&lt;/th&gt;
&lt;th&gt;特点&lt;/th&gt;
&lt;th&gt;适用场景&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;SGD&lt;/td&gt;
&lt;td&gt;基础，需手动调学习率&lt;/td&gt;
&lt;td&gt;理论研究、简单任务&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;SGD + Momentum&lt;/td&gt;
&lt;td&gt;加速收敛，减少震荡&lt;/td&gt;
&lt;td&gt;通用&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Adam&lt;/td&gt;
&lt;td&gt;自适应学习率，收敛快&lt;/td&gt;
&lt;td&gt;默认首选，尤其小数据&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;RMSProp&lt;/td&gt;
&lt;td&gt;适合非平稳目标&lt;/td&gt;
&lt;td&gt;RNN/CNN均可&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;🚀 实践建议：新手用 Adam (lr=0.001) ，高手可尝试 SGD + Momentum + 学习率衰减&lt;/p&gt;
&lt;h3&gt;4.3 正则化技术&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;L2权重衰减（Weight Decay）：损失函数加 $$ \lambda |\theta|^2 $$&lt;/li&gt;
&lt;li&gt;Dropout：训练时随机丢弃神经元（通常FC层用，rate=0.5）&lt;/li&gt;
&lt;li&gt;Early Stopping：验证集loss不再下降时停止&lt;/li&gt;
&lt;li&gt;Label Smoothing：软化one-hot标签，防过自信&lt;/li&gt;
&lt;/ul&gt;
&lt;hr /&gt;
&lt;h2&gt;五、经典CNN架构深度解析&lt;/h2&gt;
&lt;h3&gt;5.1 LeNet-5 (1998) —— 开山鼻祖&lt;/h3&gt;
&lt;p&gt;INPUT → Conv1 → Pool1 → Conv2 → Pool2 → FC1 → FC2 → OUTPUT&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;用于手写数字识别（MNIST）&lt;/li&gt;
&lt;li&gt;首次证明CNN可行性&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;5.2 AlexNet (2012) —— 深度学习革命&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;8层（5Conv + 3FC）&lt;/li&gt;
&lt;li&gt;首次使用ReLU、Dropout&lt;/li&gt;
&lt;li&gt;数据增强 + GPU训练&lt;/li&gt;
&lt;li&gt;ImageNet Top-5错误率从26%→15.3%&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;5.3 VGGNet (2014) —— 简洁之美&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;核心：统一使用3×3小卷积核堆叠&lt;/li&gt;
&lt;li&gt;感受野等价于一个大核，但参数更少&lt;/li&gt;
&lt;li&gt;VGG16：13Conv + 3FC = 16层&lt;/li&gt;
&lt;li&gt;至今仍被用作特征提取器（如风格迁移）&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;5.4 GoogLeNet / Inception (2014) —— 多尺度融合&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;Inception模块：并行使用1×1, 3×3, 5×5卷积 + Pooling → 捕捉多尺度特征&lt;/li&gt;
&lt;li&gt;1×1卷积：降维/升维，减少计算量（“瓶颈层”）&lt;/li&gt;
&lt;li&gt;22层，参数比AlexNet少12倍&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;5.5 ResNet (2015) —— 突破深度极限&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;残差块（Residual Block）：
$$y = F(x) + x$$&lt;/li&gt;
&lt;li&gt;解决“网络退化”问题（深层网络训练误差反而上升）&lt;/li&gt;
&lt;li&gt;可训练超1000层网络（ResNet-152常用）&lt;/li&gt;
&lt;li&gt;核心思想：让网络学习残差（变化量），而非直接映射&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;🏆 ResNet是现代CNN的基石，几乎所有新模型都受其影响。&lt;/p&gt;
&lt;h3&gt;5.6 EfficientNet (2019) —— 平衡的艺术&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;提出复合缩放（Compound Scaling） ：同时缩放网络深度（d）、宽度（w）、分辨率（r）&lt;/li&gt;
&lt;li&gt;公式：$$d = \alpha^\phi, w = \beta^\phi, r = \gamma^\phi$$，约束 $$ \alpha \cdot \beta^2 \cdot \gamma^2 \approx 2 $$&lt;/li&gt;
&lt;li&gt;在相同计算量下达到SOTA精度&lt;/li&gt;
&lt;/ul&gt;
&lt;hr /&gt;
&lt;h2&gt;六、CNN 在各领域的应用详解（附模型）&lt;/h2&gt;
&lt;h3&gt;6.1 图像分类 → ResNet, EfficientNet, Vision Transformer&lt;/h3&gt;
&lt;h3&gt;6.2 目标检测&lt;/h3&gt;
&lt;h4&gt;两阶段检测器（精度高）&lt;/h4&gt;
&lt;ul&gt;
&lt;li&gt;Faster R-CNN：RPN生成候选框 + CNN分类&lt;/li&gt;
&lt;li&gt;Mask R-CNN：Faster R-CNN + 分割分支&lt;/li&gt;
&lt;/ul&gt;
&lt;h4&gt;单阶段检测器（速度快）&lt;/h4&gt;
&lt;ul&gt;
&lt;li&gt;YOLO系列（You Only Look Once）：将检测视为回归问题&lt;/li&gt;
&lt;li&gt;SSD（Single Shot MultiBox Detector）：多尺度特征图预测&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;6.3 语义分割&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;FCN（全卷积网络）：将FC层替换为卷积，输出像素级预测&lt;/li&gt;
&lt;li&gt;U-Net：编码器-解码器结构 + 跳跃连接（医学图像标配）&lt;/li&gt;
&lt;li&gt;DeepLab系列：空洞卷积 + ASPP（多尺度上下文）&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;6.4 姿态估计&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;OpenPose：多阶段CNN，同时预测关键点和连接&lt;/li&gt;
&lt;li&gt;HRNet：保持高分辨率特征，精度更高&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;6.5 医学影像&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;nnU-Net：自动适配任何医学分割任务的框架&lt;/li&gt;
&lt;li&gt;CheXNet：121层DenseNet，用于胸部X光肺炎检测&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;6.6 视频理解&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;3D CNN（如C3D）：卷积核扩展到时间维&lt;/li&gt;
&lt;li&gt;Two-Stream Network：RGB流 + 光流流&lt;/li&gt;
&lt;li&gt;I3D（Inflated 3D ConvNet）：将2D卷积核“膨胀”为3D&lt;/li&gt;
&lt;/ul&gt;
&lt;hr /&gt;
&lt;h2&gt;七、CNN 的前沿发展与未来&lt;/h2&gt;
&lt;h3&gt;7.1 CNN vs Transformer&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;Vision Transformer (ViT)：将图像分块，用Transformer编码 → 在大数据下超越CNN&lt;/li&gt;
&lt;li&gt;混合模型：CNN提取局部特征 + Transformer建模长程依赖（如Swin Transformer）&lt;/li&gt;
&lt;li&gt;趋势：CNN不会消失，但与Attention机制深度融合&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;7.2 轻量化CNN&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;MobileNet系列：深度可分离卷积&lt;/li&gt;
&lt;li&gt;ShuffleNet：通道混洗减少计算&lt;/li&gt;
&lt;li&gt;GhostNet：用廉价操作生成“幻影”特征图&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;7.3 自监督学习&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;MoCo, SimCLR：无标签数据预训练CNN，再微调&lt;/li&gt;
&lt;li&gt;减少对标注数据的依赖&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;7.4 可解释性与可视化&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;CAM / Grad-CAM：可视化CNN关注区域&lt;/li&gt;
&lt;li&gt;特征反演：从特征图重建输入图像&lt;/li&gt;
&lt;/ul&gt;
&lt;hr /&gt;
&lt;h2&gt;八、学习路径与资源推荐&lt;/h2&gt;
&lt;h3&gt;8.1 学习路线图&lt;/h3&gt;
&lt;p&gt;数学基础 → Python/PyTorch → CNN理论 → 经典论文 → 复现项目 → 参加比赛（Kaggle）→ 研究前沿&lt;/p&gt;
&lt;h3&gt;8.2 书籍推荐&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;《深度学习》（花书）Ian Goodfellow — 理论基石&lt;/li&gt;
&lt;li&gt;《动手学深度学习》（李沐）— 代码实践神器（有PyTorch版）&lt;/li&gt;
&lt;li&gt;《神经网络与深度学习》（邱锡鹏）— 中文精品&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;8.3 视频课程&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;吴恩达《Deep Learning Specialization》（Coursera）&lt;/li&gt;
&lt;li&gt;李沐《动手学深度学习》B站课程&lt;/li&gt;
&lt;li&gt;斯坦福CS231n（YouTube）— CNN圣经级课程&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;8.4 实战平台&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;Kaggle：大量图像比赛（入门推荐：Digit Recognizer, Dogs vs Cats）&lt;/li&gt;
&lt;li&gt;Google Colab：免费GPU，开箱即用&lt;/li&gt;
&lt;li&gt;Hugging Face：预训练模型库（含CNN）&lt;/li&gt;
&lt;/ul&gt;
&lt;hr /&gt;
&lt;h2&gt;九、一个完整CNN项目实战框架（PyTorch）&lt;/h2&gt;
&lt;h3&gt;9.1 数据加载与增强&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;import torch
import torch.nn as nn
import torch.optim as optim
from torchvision import datasets, transforms

transform = transforms.Compose([
    transforms.Resize((32, 32)),
    transforms.ToTensor(),
    transforms.Normalize((0.5,), (0.5,))
])

trainset = datasets.CIFAR10(root=&quot;./data&quot;, train=True, download=True, transform=transform)
trainloader = torch.utils.data.DataLoader(trainset, batch_size=64, shuffle=True)
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;9.2 定义带BN和Dropout的CNN&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;class CNN(nn.Module):
    def __init__(self):
        super().__init__()
        self.features = nn.Sequential(
            nn.Conv2d(3, 32, 3, padding=1),
            nn.BatchNorm2d(32),
            nn.ReLU(),
            nn.MaxPool2d(2),
            nn.Conv2d(32, 64, 3, padding=1),
            nn.BatchNorm2d(64),
            nn.ReLU(),
            nn.MaxPool2d(2),
            nn.Conv2d(64, 128, 3, padding=1),
            nn.BatchNorm2d(128),
            nn.ReLU(),
            nn.AdaptiveAvgPool2d((1, 1)),
        )
        self.classifier = nn.Sequential(
            nn.Dropout(0.5),
            nn.Linear(128, 10),
        )

    def forward(self, x):
        x = self.features(x)
        x = torch.flatten(x, 1)
        x = self.classifier(x)
        return x
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;9.3 训练循环&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;model = CNN().cuda()
criterion = nn.CrossEntropyLoss()
optimizer = optim.Adam(model.parameters(), lr=0.001)

for epoch in range(10):
    for inputs, labels in trainloader:
        inputs, labels = inputs.cuda(), labels.cuda()
        optimizer.zero_grad()
        outputs = model(inputs)
        loss = criterion(outputs, labels)
        loss.backward()
        optimizer.step()
    print(f&quot;Epoch {epoch + 1}, Loss: {loss.item():.4f}&quot;)
&lt;/code&gt;&lt;/pre&gt;
&lt;hr /&gt;
&lt;h2&gt;十、总结：CNN 的核心价值&lt;/h2&gt;
&lt;p&gt;🧠 CNN 的本质是“空间特征的层次化自动提取器” 。&lt;/p&gt;
&lt;p&gt;它通过：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;局部连接 → 捕捉空间相关性&lt;/li&gt;
&lt;li&gt;权值共享 → 极大减少参数&lt;/li&gt;
&lt;li&gt;池化操作 → 提供不变性与降维&lt;/li&gt;
&lt;li&gt;深度堆叠 → 实现从像素到语义的抽象&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;彻底改变了计算机视觉，并辐射到NLP、语音、生物、物理等众多领域。&lt;/p&gt;
</content:encoded></item><item><title>PyTorch 学习笔记</title><link>https://hyperthreading.cn/posts/pytorch%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/</link><guid isPermaLink="true">https://hyperthreading.cn/posts/pytorch%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/</guid><pubDate>Mon, 01 Dec 2025 00:00:00 GMT</pubDate><content:encoded>&lt;h2&gt;PyTorch 核心知识点整理&lt;/h2&gt;
&lt;h2&gt;一、基础概念&lt;/h2&gt;
&lt;h3&gt;1.1 Tensor（张量）&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;核心数据结构：类似于 NumPy 的 ndarray，但支持 GPU 加速和自动求导。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;创建方式：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import torch

# 从 Python 列表或 NumPy 数组创建
x = torch.tensor([1, 2, 3])

# 创建特定值的张量
a = torch.zeros((2, 3))
b = torch.ones((2, 3))
c = torch.rand((2, 3))
d = torch.randn((2, 3))

# 创建序列
r1 = torch.arange(0, 10, 2)
r2 = torch.linspace(0, 1, 5)

# 创建单位矩阵
i = torch.eye(3)
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;属性：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;x.dtype
x.shape
x.size()
x.device
x.requires_grad
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;操作：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;索引与切片：与 NumPy 类似。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;视图 (View) 与 复制 (Copy)：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;y = x.view(-1)          # 返回一个新视图，共享底层数据（要求内存连续）
z = x.reshape(-1)       # 更灵活的形状变换，可能返回视图或副本
x_copy = x.clone()      # 返回数据的副本
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;数学运算：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;s = torch.sum(x)
m = torch.mean(x.float())
mx = torch.max(x)
e = torch.exp(x.float())
lg = torch.log(x.float())
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;广播 (Broadcasting)：自动扩展张量以进行运算。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;类型转换：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;xf = x.float()
xl = x.long()
x2 = x.to(dtype=torch.float32)
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;设备转移：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;device = torch.device(&quot;cuda:0&quot; if torch.cuda.is_available() else &quot;cpu&quot;)
x_gpu = x.to(device)
x_cpu = x_gpu.cpu()
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;1.2 自动求导（Autograd）&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;核心机制：PyTorch 能自动计算张量的梯度。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;关键属性：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import torch

# requires_grad: 设置张量需要计算梯度（通常对模型参数设置）
w = torch.randn(10, requires_grad=True)

# grad: 存储计算得到的梯度
w.grad

# grad_fn: 指向创建该张量的 Function 对象，用于构建计算图
y = w * 2
y.grad_fn
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;基本流程：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import torch

x = torch.randn(10)
w = torch.randn(10, requires_grad=True)
b = torch.randn(1, requires_grad=True)

y = (x * w).sum() + b
loss = y

loss.backward()

with torch.no_grad():
    w -= 0.01 * w.grad
    b -= 0.01 * b.grad

w.grad.zero_()
b.grad.zero_()
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;停止梯度：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import torch

x = torch.randn(3, requires_grad=True)

with torch.no_grad():
    y = x * 2

z = x.detach()
x.requires_grad_(False)
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;二、神经网络模块&lt;/h2&gt;
&lt;h3&gt;2.1 定义网络&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;常见写法：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import torch
import torch.nn as nn

class MyModel(nn.Module):
    def __init__(self):
        super().__init__()
        self.net = nn.Sequential(
            nn.Linear(10, 32),
            nn.ReLU(),
            nn.Linear(32, 10),
        )

    def forward(self, x):
        return self.net(x)
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;常用层：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import torch.nn as nn

nn.Linear
nn.Conv2d
nn.MaxPool2d
nn.AvgPool2d
nn.BatchNorm1d
nn.BatchNorm2d
nn.BatchNorm3d
nn.Dropout
nn.Embedding
nn.LSTM
nn.GRU
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;激活函数：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import torch.nn as nn

nn.ReLU
nn.Sigmoid
nn.Tanh
nn.Softmax
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;容器：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import torch.nn as nn

nn.Sequential
nn.ModuleList
nn.ModuleDict
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;2.2 损失函数（Loss Functions）&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;常用损失：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import torch.nn as nn

nn.MSELoss                 # 回归
nn.CrossEntropyLoss        # 分类（输入 logits，内部会做 Softmax）
nn.BCELoss                 # 二元交叉熵（输入概率）
nn.BCEWithLogitsLoss       # Sigmoid + BCELoss，数值更稳定
nn.NLLLoss                 # 负对数似然（常与 LogSoftmax 配合）
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;使用：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;loss = criterion(predicted_output, true_target)
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;2.3 优化器（Optimizers）&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;作用：根据计算出的梯度更新模型参数。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;常用优化器：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import torch

torch.optim.SGD
torch.optim.Adam
torch.optim.RMSprop
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;基本使用流程：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;optimizer = torch.optim.Adam(model.parameters(), lr=0.001)
loss = criterion(outputs, labels)

optimizer.zero_grad()
loss.backward()
optimizer.step()
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;三、数据处理&lt;/h2&gt;
&lt;h3&gt;3.1 Dataset&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;抽象基类：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;from torch.utils.data import Dataset
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;自定义数据集：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;from torch.utils.data import Dataset

class MyCustomDataset(Dataset):
    def __len__(self):
        return 0

    def __getitem__(self, idx):
        return None
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;内置数据集：torchvision.datasets.MNIST/CIFAR10/ImageFolder 等。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import torchvision

torchvision.datasets.MNIST
torchvision.datasets.CIFAR10
torchvision.datasets.ImageFolder
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;3.2 DataLoader&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;作用：将数据集封装成可迭代对象，支持批量加载、打乱、多进程加载。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;from torch.utils.data import DataLoader

DataLoader(dataset, batch_size=32, shuffle=True, num_workers=4)
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;使用：&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;dataset = MyCustomDataset(...)
dataloader = DataLoader(dataset, batch_size=32, shuffle=True, num_workers=4)

for batch_data, batch_labels in dataloader:
    # 训练/验证逻辑
    pass
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;四、训练与验证流程&lt;/h2&gt;
&lt;h3&gt;4.1 基本训练循环&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;model.train()  # 设置为训练模式（影响 Dropout, BatchNorm 等层）
for epoch in range(num_epochs):
    for inputs, labels in train_loader:
        inputs, labels = inputs.to(device), labels.to(device)

        optimizer.zero_grad()  # 1. 清空梯度
        outputs = model(inputs)  # 2. 前向传播
        loss = criterion(outputs, labels)  # 3. 计算损失
        loss.backward()  # 4. 反向传播
        optimizer.step()  # 5. 更新参数

        # 可选：记录损失、准确率等
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;4.2 验证/测试循环&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;model.eval()  # 设置为评估模式（关闭 Dropout, 固定 BatchNorm 统计量）
with torch.no_grad():  # 关闭梯度计算，节省内存和计算
    for inputs, labels in val_loader:
        inputs, labels = inputs.to(device), labels.to(device)
        outputs = model(inputs)
        loss = criterion(outputs, labels)
        # 计算准确率或其他指标
        # 记录验证损失和指标
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;五、模型保存与加载&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;保存模型参数：&lt;pre&gt;&lt;code&gt;torch.save(model.state_dict(), &quot;model_weights.pth&quot;)
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;li&gt;加载模型参数：&lt;pre&gt;&lt;code&gt;model = MyModel()  # 先实例化模型结构
model.load_state_dict(torch.load(&quot;model_weights.pth&quot;))
model.eval()  # 通常加载后用于推理，设为评估模式
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;li&gt;保存整个模型（不推荐，依赖具体类定义）：&lt;pre&gt;&lt;code&gt;torch.save(model, &quot;entire_model.pth&quot;)
model = torch.load(&quot;entire_model.pth&quot;)
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;六、GPU 加速&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;检查 GPU 可用性：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import torch

torch.cuda.is_available()
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;指定设备：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import torch

device = torch.device(&quot;cuda:0&quot; if torch.cuda.is_available() else &quot;cpu&quot;)
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;转移张量和模型：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;tensor = tensor.to(device)
model = model.to(device)
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;七、其他重要特性&lt;/h2&gt;
&lt;h3&gt;7.1 TorchScript（模型部署）&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;将 PyTorch 模型转换为可序列化的、独立于 Python 的中间表示，便于在 C++ 环境或生产环境中部署。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;常用方式：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import torch

torch.jit.trace(model, example_input)
torch.jit.script(model)
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;7.2 分布式训练&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;常用方式：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import torch

torch.nn.DataParallel
torch.nn.parallel.DistributedDataParallel
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;7.3 混合精度训练（AMP）&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;常用工具：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import torch

torch.cuda.amp.autocast
torch.cuda.amp.GradScaler
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;h3&gt;7.4 可视化与调试&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;TensorBoard：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;from torch.utils.tensorboard import SummaryWriter
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;打印模型结构：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;print(model)
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;结构摘要（需安装第三方包）：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;summary(model, input_size)
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;八、常用工具库&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;TorchVision：提供图像数据集、模型架构、图像变换工具。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import torchvision
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;TorchText：提供文本数据集和预处理工具（NLP）。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import torchtext
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;TorchAudio：提供音频数据集和处理工具。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import torchaudio
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;九、最佳实践&lt;/h2&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;总是清空梯度：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;optimizer.zero_grad()
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;区分训练和评估模式：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;model.train()
model.eval()

with torch.no_grad():
    pass
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;使用数据加载器：进行批量、打乱和并行数据加载。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;from torch.utils.data import DataLoader

dataloader = DataLoader(dataset, batch_size=32, shuffle=True, num_workers=4)
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;设备管理：显式地将模型和数据移动到目标设备。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;inputs = inputs.to(device)
labels = labels.to(device)
model = model.to(device)
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;保存和加载模型参数：优先保存/加载参数字典，而不是整个模型对象。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;torch.save(model.state_dict(), &quot;model_weights.pth&quot;)
model.load_state_dict(torch.load(&quot;model_weights.pth&quot;))
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;设置随机种子：保证实验的可复现性。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;import torch
import numpy as np

torch.manual_seed(42)
np.random.seed(42)
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;利用自动混合精度：在支持的 GPU 上加速训练。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;from torch.cuda.amp import autocast, GradScaler

scaler = GradScaler()
with autocast():
    loss = criterion(outputs, labels)
scaler.scale(loss).backward()
scaler.step(optimizer)
scaler.update()
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;监控训练过程：记录损失、准确率、学习率等。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;from torch.utils.tensorboard import SummaryWriter

writer = SummaryWriter()
writer.add_scalar(&quot;loss/train&quot;, loss.item(), global_step)
writer.flush()
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;hr /&gt;
</content:encoded></item></channel></rss>